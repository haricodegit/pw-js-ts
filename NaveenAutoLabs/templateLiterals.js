//#34 Template literals in JavaScript || Backtick concept

const name = "Naveen";

//Template literals - Backtick concept ``

const age = 30

const mesg = `Hello, My name is ${name} and age is ${age}`
console.log(mesg)

//printing paragraphs, multi-line string

const multiLine = `Hi, this is JavaScript code
 and I'm so happy
 and I want to be perfect in JS
 oh yes!`

 console.log(multiLine);

 //Maths expressions with backticks or Template literals

 const a = 10;
 const b = 20;

 const res = `Sum of ${a} and ${b} is ${a+b}`
 console.log(res);